import { Module } from '@nestjs/common';

/**
 * Subscription Detection Context (existing in v1).
 * Recurring payment detection, unused subscription flagging.
 */
@Module({})
export class SubscriptionsModule {}
